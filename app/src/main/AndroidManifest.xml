<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          
    package="org.adblockplus.android"
    >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_SUPERUSER" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".AdblockPlus"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.preference.Preferences"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.preference.AdvancedPreferences"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.ConfigurationActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:launchMode="singleInstance"
            android:theme="@android:style/Theme.Holo.Dialog" />
        <activity
            android:name=".activity.ProxyConfigurationActivity"
            android:excludeFromRecents="true"
            android:label="@string/proxysettings_name"
            android:launchMode="singleInstance"
            android:theme="@android:style/Theme.Holo.Dialog" />
        <activity
            android:name=".activity.dialog.CrashReportDialog"
            android:excludeFromRecents="true"
            android:label="@string/crash_name"
            android:launchMode="singleInstance"
            android:theme="@android:style/Theme.Holo.Dialog" />
        <activity
            android:name=".activity.UpdaterActivity"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent" />

        <service android:name=".service.ProxyService" />
        <service android:name=".service.UpdaterService" />

        <receiver android:name=".Starter" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_REPLACED" />

                <data android:scheme="package" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
